<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tintash.firstapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".JavaActivity1">

        </activity>
        <activity android:name=".JavaActivity2">
        </activity>
        <activity android:name=".KotlinActivity1">
        </activity>

        <activity android:name=".KotlinActivity2"></activity>
        <activity android:name="LifeCycleTest">
        </activity>
        <activity android:name=".ServiceActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".ExampleService">

        </service>
    </application>

</manifest>